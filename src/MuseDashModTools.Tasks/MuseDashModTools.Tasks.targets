<Project>
    <PropertyGroup>
        <AssemblyPath>$(SolutionDir)bin\ExternalBuilds\$(MSBuildThisFileName).dll</AssemblyPath>
    </PropertyGroup>

    <UsingTask TaskName="$(MSBuildThisFileName).GenerateModToolsStyles"
               AssemblyFile="$(AssemblyPath)"/>
</Project>